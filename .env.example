# OpenAI
OPENAI_API_KEY=sk-...

# Anthropic
ANTHROPIC_API_KEY=sk-ant-...

# Google Vision (path to service account JSON key file)
GOOGLE_APPLICATION_CREDENTIALS=/path/to/credentials.json

# Local AI model cache directory (optional, default: ~/.cache/imganalyzer)
# IMGANALYZER_MODEL_CACHE=/path/to/model/cache

# Default AI backend: openai | anthropic | google | local | none
# IMGANALYZER_DEFAULT_AI=none

# ── Local AI: Object Detection ────────────────────────────────────────────────
# Custom GroundingDINO detection prompt — categories separated by ' . '
# Overridden per-run by --detection-prompt flag
# IMGANALYZER_DETECTION_PROMPT=person . animal . vehicle . building . tree . food .

# Object detection confidence threshold (0.0–1.0, default: 0.30)
# Overridden per-run by --detection-threshold flag
# IMGANALYZER_DETECTION_THRESHOLD=0.30

# ── Local AI: Face Recognition ────────────────────────────────────────────────
# Face recognition cosine similarity threshold (0.0–1.0, default: 0.40)
# Higher = stricter matching (fewer false positives)
# Overridden per-run by --face-threshold flag
# IMGANALYZER_FACE_DB_THRESHOLD=0.40

# Path to the face embeddings database JSON file
# (default: ~/.cache/imganalyzer/faces.json)
# IMGANALYZER_FACE_DB=~/.cache/imganalyzer/faces.json
